/**
 * Placeholder surface. The colour maths land in #3 and the components in #4-#6;
 * this exists so the build, the type emit and the playground link have something
 * real to resolve while the scaffold is proven.
 */
export const PACKAGE_NAME = 'vue-tray-color-picker'
